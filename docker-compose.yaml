version: '3'

services:
  frontend:
    build: ./frontend-app
    volumes:
      - ./frontend-app:/app
    working_dir: /app
    command: npm start
    ports:
      - 4200

  frontend.console:
    build: ./frontend-app
    command: bash
    volumes:
      - ./frontend-app:/app
    working_dir: /app

  backend:
    build: ./backend-app
    volumes:
      - ./backend-app:/app
    working_dir: /app
    command: npm start
    ports:
      - 3000

  backend.console:
    build: ./backend-app
    command: bash
    volumes:
      - ./backend-app:/app
    working_dir: /app

  redis:
    image: redis
  redis.console:
    image: redis
    command: redis-cli -h redis
    links:
      - redis
  mongo:
    image: mongo

  mongo.console:
    image: mongo
    command: mongo --host mongo
    links:
      - mongo
